#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../hogan.js/bin/hulk" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../hogan.js/bin/hulk" "$@"
  ret=$?
fi
exit $ret
